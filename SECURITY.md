# Security policy

Do not report vulnerabilities in public issues. Report privately to the maintainers with reproduction steps, impact, and affected files. Never include credentials in a report.

The pipeline accepts no private repositories, credentials, arbitrary URLs, or executable source content. GitHub Actions secrets are used only for explicitly configured publishing and are never printed. Supported reports include source-policy bypass, provenance leakage, malicious catalog data, and release-integrity failures.
